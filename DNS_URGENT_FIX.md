# 🚨 緊急: DNS設定が完了していません

現在、`jigsawpuzzlesimple.com` のDNS設定が完了していないため、サイトにアクセスできません。

## 現在の状況

- ✅ GitHub Pagesのデプロイは成功しています
- ❌ DNS設定が完了していないため、カスタムドメインでアクセスできません
- ✅ 一時的にGitHub PagesのデフォルトURLでアクセス可能です

## 一時的なアクセス方法

デプロイは成功しているため、以下のURLでサイトにアクセスできます：

**https://yuharih.github.io/simplejigsawpuzzle/**

このURLはDNS設定が完了するまで使用できます。

## DNS設定を完了させる手順

### ステップ1: ドメインプロバイダーの管理画面にアクセス

`jigsawpuzzlesimple.com` を購入したプロバイダー（お名前.com、ムームードメイン、GoDaddyなど）の管理画面にログインしてください。

### ステップ2: DNS設定画面を開く

ドメイン管理画面で以下のメニューを探してください：
- **DNS設定**
- **DNSレコード設定**
- **ネームサーバー設定**
- **DNS管理**

### ステップ3: Aレコードを4つ追加

以下の4つのAレコードを**すべて追加**してください：

| レコードタイプ | ホスト名 | 値（IPアドレス） | TTL |
|------------|---------|----------------|-----|
| A | `@` または空白 | `185.199.108.153` | 3600 |
| A | `@` または空白 | `185.199.109.153` | 3600 |
| A | `@` または空白 | `185.199.110.153` | 3600 |
| A | `@` または空白 | `185.199.111.153` | 3600 |

**重要**: 
- ホスト名は `@` または空白（ルートドメインを表す）
- 4つすべてのIPアドレスを追加してください
- TTLは3600（1時間）が推奨です

### ステップ4: DNS設定を保存

設定を保存してください。プロバイダーによっては「設定する」「保存」「反映」などのボタンをクリックします。

### ステップ5: GitHub Pagesの設定を確認

1. GitHubリポジトリ `https://github.com/yuharih/simplejigsawpuzzle` にアクセス
2. **Settings** > **Pages** を開く
3. **Custom domain** に `jigsawpuzzlesimple.com` が入力されているか確認
4. 入力されていない場合は、`jigsawpuzzlesimple.com` を入力して **Save** をクリック

### ステップ6: DNS設定の反映を待つ

- DNS設定の変更は**30分〜数時間**で反映されます
- 最大48時間かかることがありますが、通常は数時間以内です

### ステップ7: DNS設定の確認

以下のコマンドでDNS設定が反映されたか確認できます：

```bash
dig jigsawpuzzlesimple.com +short
```

正しく設定されていれば、以下のIPアドレスのいずれかが表示されます：
- `185.199.108.153`
- `185.199.109.153`
- `185.199.110.153`
- `185.199.111.153`

または、Webツールで確認：
- https://www.whatsmydns.net/ で `jigsawpuzzlesimple.com` を検索

### ステップ8: GitHub Pagesの検証を確認

DNS設定が反映されると、GitHub Pagesの設定画面で以下が表示されます：

- ✅ **DNS check successful** - DNS設定が正しく検証されました
- ✅ **Certificate is valid** - SSL証明書が有効です

この状態になったら、**Enforce HTTPS** にチェックを入れてください。

## プロバイダー別の設定例

### お名前.com の場合

1. ドメイン一覧から `jigsawpuzzlesimple.com` を選択
2. **DNS関連機能の設定** をクリック
3. **DNSレコード設定を利用する** を選択
4. **Aレコード** を4つ追加（上記のIPアドレス）
5. **設定する** をクリック

### ムームードメイン の場合

1. ドメイン一覧から `jigsawpuzzlesimple.com` を選択
2. **DNS設定** タブを開く
3. **Aレコード** を4つ追加（上記のIPアドレス）
4. **設定を反映** をクリック

### GoDaddy の場合

1. マイプロダクトから `jigsawpuzzlesimple.com` を選択
2. **DNS** タブを開く
3. **レコード** セクションで **追加** をクリック
4. **Aレコード** を4つ追加（上記のIPアドレス）
5. **保存** をクリック

## 確認コマンド

設定が完了したら、以下のコマンドで確認できます：

```bash
# DNSレコードの確認
dig jigsawpuzzlesimple.com +short

# 詳細なDNS情報
dig jigsawpuzzlesimple.com ANY +noall +answer

# 別の方法
nslookup jigsawpuzzlesimple.com
```

正しく設定されていれば、GitHub PagesのIPアドレス（185.199.108.153など）が表示されます。

## よくある質問

### Q: DNS設定を追加したが、まだエラーが表示される

A: DNS設定の反映には時間がかかります。30分〜1時間待ってから再確認してください。

### Q: どのプロバイダーで設定すればいい？

A: ドメインを購入したプロバイダーの管理画面で設定します。

### Q: Aレコードは4つすべて必要？

A: はい。冗長性のため4つすべて追加してください。

### Q: ドメインをまだ購入していない場合は？

A: まずドメインを購入する必要があります。お名前.com、ムームードメイン、GoDaddyなどで購入できます。

## 次のステップ

1. ドメインプロバイダーの管理画面でDNS設定を完了させる
2. 30分〜1時間待つ
3. `dig jigsawpuzzlesimple.com +short` でDNS設定を確認
4. GitHub Pagesの設定画面で「DNS check successful」と表示されるか確認
5. `https://jigsawpuzzlesimple.com` にアクセス

DNS設定が完了するまで、一時的に `https://yuharih.github.io/simplejigsawpuzzle/` でアクセスできます。

